# GNAS-lite 1.1

GNAS-lite は、小規模な修正や短いセッション向けに GNAS 1.1 の必須安全規則だけを残した簡略版です。大規模・長期・複数Workstreamの開発では `Agents.md` のFull profileを使用してください。

## 1. 基本原則

1. 人間が依頼した範囲だけを変更する。
2. 変更前にGitの現在地と作業ツリーを確認する。
3. 実装後は必ずビルドまたはテストで検証する。
4. 検証できない変更を「完了」としない。
5. 途中停止しても再開できるよう `resume/current.md` を更新する。
6. ユーザー所有のビルド設定Markdownは読み取り専用とする。

## 2. 最小ファイル構成

```text
build-tools.md          # 任意。ユーザー所有。エージェントは原則読み取り専用
resume/
├── current.md          # 作業中のみ存在する再開ポイント
└── history/            # 任意。過去の再開記録
```

`build-tools.md` の代わりに別のビルド設定Markdownをユーザーが指定した場合は、そのパスを使用します。

## 3. 開始手順

1. `resume/current.md` があれば最初に読む。
2. `git status`、現在のブランチ、HEADを確認する。
3. `build-tools.md` またはユーザー指定の設定MDがあれば読む。**編集しない**。
4. 依頼された変更範囲と検証方法を確認する。
5. 必要なら作業ブランチまたはチェックポイントを作る。

## 4. 実行

作業中は、次のような節目で `resume/current.md` を更新します。

- 大規模編集の前後
- 長いビルド・テストの前後
- 外部APIを連続して呼ぶ前後
- エラーが発生したとき
- レート制限を検知したとき
- ユーザーまたはシステム都合で停止する可能性が高いとき

### resume/current.md 最小テンプレート

```markdown
# GNAS Resume

- Status: running
- Branch:
- HEAD:
- Task:
- Completed:
- Next:
- Changed files:
- Last successful verification:
- Blocking error / rate limit:
- Safe first command on resume:
```

## 5. ビルド失敗時

ビルド失敗を単なる終了理由にせず、次の順で扱います。

1. 実行コマンド、終了コード、主要エラーを保存。
2. コンパイラ、SDK、CMake、Node.js、Python等のバージョンを必要に応じて確認。
3. ソース不整合、依存関係不足、ツールチェーン不一致、PATH/権限、OS依存、ネットワーク/レート制限のどれかを切り分ける。
4. 非破壊的な確認・修復を優先する。
5. 自動解決できなければ、原因候補・根拠・具体的な解決手順をユーザーへ提示する。
6. 危険な削除、広範なアンインストール、OS全体の設定変更は明示許可なしに行わない。

## 6. レート制限・途中停止

レート制限や停止を検知した場合、無限に再試行しません。`resume/current.md` に次を残します。

- どこまで完了したか
- 何が未完了か
- 制限またはエラーの内容
- 再開条件
- 再開後の最初の安全な操作

Git上の状態とResumeの記録が一致しない場合は、Gitの事実を優先して差分を確認します。

## 7. 完了

1. ビルドまたはテストを実行する。
2. 成功結果を確認する。
3. 変更内容を報告する。
4. `resume/current.md` を削除するか `resume/history/` へ移す。
5. 未解決事項があれば明記して停止する。

---

GNAS-lite は速度と簡潔さを優先しますが、**検証・復帰可能性・ユーザー所有設定の保護**は省略しません。
