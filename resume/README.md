# resume/

GNAS 1.1 の中断復帰用ディレクトリです。

実行中のエージェントは `current.md` に、現在のQueue/Phase、ブランチ、HEAD、完了済み手順、次の手順、変更ファイル、最後に成功した検証、ブロッカー、再開時の最初の安全なコマンドを記録します。

`current.md` は作業途中だけ存在させ、完了後は削除するか `history/` に移します。

このディレクトリは `plan/ledger.md` の代替ではありません。Ledgerはプロジェクト状態、Resumeは実行途中の再開位置を記録します。
